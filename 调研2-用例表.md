# 超算作业异常用例表（对照调研 2）

来源：`调研2.md` 中的 23 类报错。本表按 **sidecar 可观测的 `reason_code`** 分行，供异常故障用例库选型，不是把调研原文再抄一遍。

约定：

- sidecar 只包装用户作业步；计算节点工具确定性采集；不调用 `scancel` / `scontrol`；不覆盖 `reason_code`。
- 调研第 2 节的「OOM」拆成内核 OOM 与 cgroup OOM；第 6 节拆出已有的 MPI_Abort；第 7 节拆出温和 I/O 失败与未实现的 `io_stall`。
- **分层**：已有 / P0 用户态可复现 / P1 注入假证据测分类 / P2 能写程序但缺专用码 / 超出契约。
- **用例库目录名 = 总表 ID**（一位数补零：`01`…`09`）。`2a` 与 `2b` 合成 `examples/02/`。硬件 `18`–`23` 保持空目标。健康对照 `mpi_io_load.c` 留在 `examples/` 根目录，不算总表 ID。
- 集群断言：`scripts/run_fault_case.sh <id>` 调 `sidecar.sh srun` 后跑 `scripts/assert_telemetry.py`。无 `srun` 则 skip（exit 2）。覆盖项可用 `AGENT_BAD_PARTITION`、`AGENT_CASE_MEM`、`AGENT_CASE_TIME`。

## 按总表 ID 是否完成

| 目录 | 总表 ID | 内容 | 是否完成 | 子编译 |
| --- | --- | --- | --- | --- |
| `examples/01/` | 1 | 缺动态库 | **完成** | `all` → `mpi_missing_so` |
| `examples/02/` | 2a + 2b | 内核 OOM / cgroup OOM | **完成**（2a 仅单测） | `all` → `cgroup_oom` |
| `examples/03/` | 3 | 段错误 | **完成** | `all` → `mpi_fault_segfault`；`npb` 需 `NPB_MPI_ROOT` |
| `examples/04/` | 4 | CRLF | **完成** | CRLF `job.sh` |
| `examples/05/` | 5 | SIGFPE | **完成** | `all` → `mpi_fault_fpe` |
| `examples/06/` | 6 | MPI 死锁 | **完成** | `all` → `mpi_fault_deadlock` |
| `examples/06x/` | 6x | MPI_Abort | **完成** | `all` → `mpi_fault_abort` |
| `examples/07a/` | 7a | 应用 I/O 失败 | **完成** | `all` → `io_enoent` |
| `examples/07b/` | 7b | I/O 空转 | 未完成 | 空 |
| `examples/08/` | 8 | 数值发散 exit 0 | 未完成 | 空 |
| `examples/09/` | 9 | OpenMP 超订 | 未完成 | 空 |
| `examples/10/` | 10 | 启动失败 | **完成**（无二进制） | 缺失路径 `10/no-such-mpi` |
| `examples/11a/` | 11a | 非法 srun 参数 | **完成**（无二进制） | 推荐 `srun -p __no_such_partition__` |
| `examples/11b/` | 11b | 超订变慢 | 未完成 | 空 |
| `examples/12/` | 12 | TimeLimit | **完成** | `sleep_timeout.sh` |
| `examples/13/` | 13 | cgroup 内存超限 | **完成** | 复用 `02/` |
| `examples/14/` | 14 | Pending | 未完成 | 空 |
| `examples/15/` | 15 | Prolog/Epilog | 仅单测注入 | 空目标；假 `NODE_FAIL` |
| `examples/16/` | 16 | 环境未 inherit | **完成** | 复用 `01/` |
| `examples/17/` | 17 | drain/down | 未完成 | 空 |
| `examples/18/` | 18 | 节点宕机 | 未完成 | 空（硬件本轮不动） |
| `examples/19/` | 19 | 网络/IB | 未完成 | 空 |
| `examples/20/` | 20 | Lustre/OST | 未完成 | 空 |
| `examples/21/` | 21 | GPU | 未完成 | 空 |
| `examples/22/` | 22 | 过热 | 未完成 | 空 |
| `examples/23/` | 23 | 电源 | 未完成 | 空 |
| `examples/mpi_io_load.c` | — | 健康 IO→CPU | 对照（非故障） | 顶层 `make mpi_io_load` |

配套：`scripts/run_fault_case.sh`、`scripts/assert_telemetry.py`；旧 demo `demo_mpi_segfault.sh`、`demo_npb_cg_segfault.sh`、`demo_mpi_abort.sh`、`demo_opencode_launch_fail.sh`、`demo_job_assist_mpi.sh`。

## 总表

| ID | 目录 | 是否完成 | 层 | 用例名 | 源码 | 调研要点（触发/表象） | 期望主码 | 分析 pack | 建议做法 | 分层 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `01/` | 完成 | 应用 | 缺动态库 | `01/mpi_missing_so.c` | `ldd` not found / `cannot open shared object` | `execution_error`（`pid_count=0` 时 pack=`launch_fail`） | `launch_fail` | 链未部署的 `.so` | 已有 |
| 2a | `02/` | 完成（仅单测） | 应用 | 内核 OOM Killer | 与 2b 同目录 | malloc 撑爆节点，`dmesg` Killed process | `node_local` | stub | **禁止 srun demo**；注入 node-diag | P1 |
| 2b | `02/` | 完成 | 调度 | Slurm cgroup OOM | `02/cgroup_oom.c` | `OUT_OF_MEMORY`，不等于内核 OOM | `slurm_oom` | stub | 小 `--mem` + 可控 malloc | 已有 |
| 3 | `03/` | 完成 | 应用 | 段错误 | `03/mpi_fault_segfault.c`；`03/npb_cg_mid_segfault/` | SIGSEGV / 空指针 / 越界 | `mpi_segfault` | `mpi_segfault` | 已覆盖 | 已有 |
| 4 | `04/` | 完成 | 应用 | CRLF / 隐藏字符 | `04/job.sh` | `bad interpreter`、`^M` | `execution_error` | 或 `launch_fail` | 故意 CRLF 的 `job.sh` | 已有 |
| 5 | `05/` | 完成 | 应用 | 浮点异常 SIGFPE | `05/mpi_fault_fpe.c` | 除零、`raise(SIGFPE)` | **`mpi_fpe`** | stub | 打印 `rank N fpe` 后 SIGFPE | 已有 |
| 6 | `06/` | 完成 | 应用 | MPI 死锁 / 挂起 | `06/mpi_fault_deadlock.c` | 一 rank 跳过 Barrier | **`mpi_deadlock`**（优先于 `timeout`） | stub | 短 hang + 很小 `--time` | 已有 |
| 6x | `06x/` | 完成 | 应用 | MPI_Abort | `06x/mpi_fault_abort.c` | 一 rank `MPI_Abort` | `mpi_abort` | `mpi_abort` | 已覆盖 | 已有 |
| 7a | `07a/` | 完成 | 应用 | 应用 I/O 失败 | `07a/io_enoent.c` | 路径 / 权限 / 配额 | `execution_error` | generic | ENOENT `open` | 已有 |
| 7b | `07b/` | 未完成 | 应用 | I/O 空转 | — | `io_stall` 未实现 | spec 有码 | stub | 先不要当必跑用例 | 超出（当前） |
| 8 | `08/` | 未完成 | 应用 | 数值发散但退出 0 | — | 残差 NaN，exit 0 | `ok` | 无故障 pack | 工具不看科学对错 | 超出（故障库） |
| 9 | `09/` | 未完成 | 应用 | OpenMP 超订变慢 | — | `OMP_NUM_THREADS` 过大 | 无码 | 无 | 不要当 tool anomaly | 超出（故障库） |
| 10 | `10/` | 完成 | 调度 | 可执行文件不存在 | `10/launch_fail.sh`；`10/no-such-mpi` | ENOENT | `execution_error` 且 `pid_count=0` | `launch_fail` | 已覆盖 | 已有 |
| 11a | `11a/` | 完成 | 调度 | 非法 srun 参数 | Makefile 记录推荐命令 | Invalid partition | `execution_error` | launch_fail / generic | `srun -p __no_such_partition__` | 已有 |
| 11b | `11b/` | 未完成 | 调度 | 合法但超订变慢 | — | `--ntasks` 与核数不匹配 | 无失败码 | 无 | 同 9 | 超出 |
| 12 | `12/` | 完成 | 调度 | TimeLimit | `12/sleep_timeout.sh` | `DUE TO TIME LIMIT` | `timeout` | generic | `sleep` + 极短 `--time` | 已有 |
| 13 | `13/` | 完成 | 调度 | 内存超限（cgroup） | 复用 `02/cgroup_oom.c` | `sacct` OUT_OF_MEMORY | `slurm_oom` | stub | 小 `--mem` + malloc | 已有 |
| 14 | `14/` | 未完成 | 调度 | Pending 过久 | — | squeue Resources / Priority | 无码 | 无 | 不包进 `srun` 作业库 | 超出 |
| 15 | `15/` | 完成（仅单测） | 调度 | Prolog / Epilog 失败 | — | `NODE_FAIL` | `node_fail` | stub | 注入假 sacct；禁止打节点 | P1 |
| 16 | `16/` | 完成 | 调度 | 环境未 inherit | `16/run_export_none.sh` | 缺 `module` / `LD_LIBRARY_PATH` | 同 1 | `launch_fail` | `--export=NONE` + 01 二进制 | 已有 |
| 17 | `17/` | 未完成 | 调度 | 节点 drain / down | — | 一直 pending | 无 | 无 | 禁止 `scontrol drain` | 超出 |
| 18 | `18/` | 未完成 | 硬件 | 节点宕机 / MCE | — | `NODE_FAIL` | `node_fail` | stub | 假 accounting | 硬件本轮不动 |
| 19 | `19/` | 未完成 | 硬件 | 节点不可达 / IB | — | `Connection timed out` | 无 IB 码 | generic | 注入 stderr 短语 | 硬件本轮不动 |
| 20 | `20/` | 未完成 | 硬件 | Lustre / OST 故障 | — | OST degraded | 或 `node_local` | stub | 注入 diag | 硬件本轮不动 |
| 21 | `21/` | 未完成 | 硬件 | GPU ECC / 掉卡 | — | XID / ECC | 无 GPU 工具 | 无 | 现阶段不进库 | 超出 |
| 22 | `22/` | 未完成 | 硬件 | 过热降频 | — | 变慢后可能 `timeout` | 无温度码 | 无 | 不制造热故障 | 超出 |
| 23 | `23/` | 未完成 | 硬件 | 电源掉电 | — | 整柜断电 | 事后 `node_fail` | stub | 同 18 | 硬件本轮不动 |

## 分层摘要

本轮完成应用/调度夹具 `01` `02`(2b) `03` `04` `05` `06` `06x` `07a` `10` `11a` `12` `13` `16`，以及 2a/15 单测注入。硬件 `18`–`23` 仍为空目标。`mpi_io_load` 在 `examples/` 根上，不是故障 ID。

| 分层 | 条目 |
| --- | --- |
| example 已做 | 1 / 16 缺库、2b/13 cgroup OOM、3 段错误、4 CRLF、5 SIGFPE、6 死锁、6x Abort、7a ENOENT、10 启动失败、11a 非法分区、12 超时 |
| 仅单测注入 | 2a `node_local`（禁止 srun）、15 `node_fail` |
| 仅健康对照 | `mpi_io_load`（不对应故障 ID） |
| 本轮不做 | 7b `io_stall`、8 NaN、9 / 11b 超订、14 Pending、17 drain、18–23 硬件 |

## 用例记录建议（入库时）

每个正式用例固定四项，与 `telemetry.json` 对齐：

1. 触发方式（用户 `srun` 二进制或短脚本，共享路径，无特权）
2. 期望主码（只允许已有或即将合入的 `reason_code`）
3. 期望证据（`events/stderr.tail`、accounting 片段、marker JSONL、是否出竖线）
4. 期望 pack（`mpi_abort` / `mpi_segfault` / `launch_fail` / `stub` / `generic`）
