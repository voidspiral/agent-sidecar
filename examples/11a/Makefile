# 11a — 总表 11a 非法 srun 参数（已完成，无二进制）
# 推荐：srun -p __no_such_partition__ hostname
# 分区名可用 AGENT_BAD_PARTITION 覆盖。

.PHONY: all clean

all:
	@echo "11a bad partition: srun -p __no_such_partition__ hostname"

clean:
	@true
