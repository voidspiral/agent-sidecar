# 异常用例库（集群测试）

登录节点 `mn`。目录名 = 调研2 总表 ID。夹具在
`/shared/agent-sidecar/examples/<id>/`。

两条入口与 [测试.md](测试.md) 相同：

- `sidecar.sh [--agent-*] srun <原生 slurm> <用户命令>`
- `sidecar-analy.sh --log DIR --code SRC`

sidecar 开关写在 `srun` **之前**。`srun` 后面就是原生 SLURM + 用户命令。
脚本自己设 `PYTHONPATH`。查 run 目录用 `sidecar.sh report`。

本机本次测试（2026-09-22）环境：

- 分区 `test`，节点 `cn[1-2]`（无 `cn3`）
- `salloc -N2 -n2 -w cn[1-2] -p test`
- **Slurm accounting 未开**（`sacct` 报 `Slurm accounting storage is disabled`），
  因此 `slurm-tap` 往往只有 `scontrol` 的 `RUNNING`，步级 TIMEOUT / OOM 不一定
  能滚成 `timeout` / `slurm_oom`
- 节点 `Memory=1200`，`--mem` **不强制 cgroup**（64M 分配里仍能 malloc 256MB）
- 批量入口：`bash /shared/agent-sidecar/scripts/run_fault_case.sh <id>`
  （无 `srun` 则 exit 2 skip）。断言失败必须非零退出。
- 覆盖：`AGENT_BAD_PARTITION`、`AGENT_CASE_MEM`、`AGENT_CASE_TIME`

```shell
unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
CC=mpicc make -C /shared/agent-sidecar/examples
```

作业结束后：

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
echo "RUN=$RUN"
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
python3 /shared/agent-sidecar/scripts/assert_telemetry.py --run-dir "$RUN" --reason <CODE>
```

Live plot：`ssh -L 8765:127.0.0.1:8765 mn`，或集群直连
[agent live plot](http://192.168.182.136:8765/)。

## 本次结果一览

| ID | 用例 | 期望主码 | 实测主码 | RUN | 结论 |
| --- | --- | --- | --- | --- | --- |
| 01 | 缺动态库 | `execution_error` + `launch_fail` | `execution_error` pid=0 pack=`launch_fail` | `20260922T030046Z-0` | **通过** |
| 02 / 2b | cgroup malloc | `slurm_oom` | `ok` rss≈258MB | `20260922T030052Z-0` | 夹具跑通；**站点不强制内存 cgroup** |
| 2a | 内核 OOM | `node_local` | — | — | **未做 srun**（禁止撑节点，仅 unittest） |
| 03 | 段错误 | `mpi_segfault` | `mpi_segfault` pid=2 | `20260922T030133Z-0` | **通过** |
| 04 | CRLF `job.sh` | `execution_error` | `execution_error` pid=0 pack=`launch_fail` | `20260922T030148Z-0` | **通过** |
| 05 | SIGFPE | `mpi_fpe` | `mpi_fpe` pid=2 pack=`stub` | `20260922T030156Z-0` | **通过** |
| 06 | MPI 死锁 | `mpi_deadlock` | `mpi_deadlock` pid=2 pack=`stub` | `20260922T030210Z-0` | **通过**（rollup 压过步级 TIME LIMIT） |
| 06x | MPI_Abort | `mpi_abort` | `mpi_abort` pid=2 | `20260922T030322Z-0` | **通过** |
| 07a | ENOENT `open` | `execution_error` | `execution_error` pid=0 pack=`launch_fail` | `20260922T030332Z-0` | 语义通过；退出太快未采到 PID |
| 07b | I/O 空转 | — | — | — | **未实现**：`io_stall` 时段检测未做 |
| 08 | 数值发散 exit 0 | — | — | — | **未实现**：工具不把科学对错当故障 |
| 09 | OpenMP 超订 | — | — | — | **未实现**：变慢不是 tool anomaly |
| 10 | 可执行文件不存在 | `execution_error` + `launch_fail` | `execution_error` pid=0 pack=`launch_fail` | `20260922T030022Z-0` | **通过** |
| 11a | 非法分区 | `execution_error` | 见下 | `20260922T030725Z-0` | **须在 salloc 外** `srun -p` |
| 11b | 合法超订变慢 | — | — | — | **未实现**：同 09 |
| 12 | TimeLimit | `timeout` | `execution_error` exit=143 | `20260922T030346Z-0` | 步已被 TIME LIMIT 杀掉；无 sacct 滚不成 `timeout` |
| 13 | cgroup 超限 | `slurm_oom` | `ok` rss≈258MB | `20260922T030520Z-0` | 同 02 |
| 14 | Pending | — | — | — | **未实现**：不包进 `srun` 作业步 |
| 15 | Prolog/Epilog | `node_fail` | — | — | **未做 srun**：仅注入假 `NODE_FAIL`，禁止打节点 |
| 16 | `--export=NONE` | 同 01 | `execution_error` pid=0 pack=`launch_fail` | `20260922T030558Z-0` | **通过** |
| 17 | drain/down | — | — | — | **未实现**：禁止 `scontrol drain` |
| 18–23 | 硬件 | — | — | — | **未实现**：本轮空目标，不打 GPU/IB/Lustre/过热/断电 |

---

# 01 缺动态库

`-Wl,-rpath` 指向从不部署的 `not-deployed-lib/libmissing.so`。
`--agent-match=mpi_missing_so`。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/01

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_missing_so srun -n1 \
    /shared/agent-sidecar/examples/01/mpi_missing_so
```

或：`bash /shared/agent-sidecar/scripts/run_fault_case.sh 01`

期望：`reason_code=execution_error`，`pid_count=0`，pack=`launch_fail`；
stderr 含 `cannot open shared object file`。

实测：`RUN=/shared/agent-runs/20260922T030046Z-0`，exit=127，**通过**。

---

# 02 / 2b cgroup malloc（Slurm OOM）

`cgroup_oom` 默认 malloc 256MiB 并触页。2a 内核 OOM **不要**用本二进制去撑节点。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/02

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N1 -n1 -w cn1 -p test --mem=16M \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=cgroup_oom srun -n1 --mem=16M \
    /shared/agent-sidecar/examples/02/cgroup_oom
```

期望（站点强制 cgroup 时）：`reason_code=slurm_oom`，pack=`stub`，`pid_count>0`。

实测（2026-09-22 mn）：

- `RUN=/shared/agent-runs/20260922T030052Z-0`：`--mem=16M` 写在 **salloc 内层 srun**，
  进程跑完 exit=0，rss_peak_mb≈257.9，主码 **`ok`**
- 再试 `salloc --mem=64M`（`20260922T030756Z-0`）：sidecar overlap 仍 `--mem=256M`，
  用户仍 malloc 成功。本集群 **不强制 Memory cgroup / accounting 关闭**，
  无法用 `--mem` 得到 `OUT_OF_MEMORY`

因此 2b **夹具可用，本站点断言 `slurm_oom` 不成立**。不要为了出码去打满 1200MB 节点
（那是 2a 内核 OOM）。

---

# 2a 内核 OOM Killer

**未实现为 srun demo。** 原因：会打节点、触发内核 OOM killer，契约禁止。
分类用 unittest 注入 `Out of memory: Killed process …` → `node_local`
（`tests/test_node_diag.py`）。

---

# 03 段错误

`3 0` = 工作约 3 秒后 rank 0 空指针。`--agent-match=mpi_fault_segfault`。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/03

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_fault_segfault srun -n2 \
    /shared/agent-sidecar/examples/03/mpi_fault_segfault 10 0
```

作业结束后 `--code` 指 examples 树：

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/agent-sidecar/examples
```

期望：`reason_code=mpi_segfault`，`pid_count>0`；stderr 含 `rank 0 segfault`。

实测：`RUN=/shared/agent-runs/20260922T030133Z-0`，exit=139，pid=2，**通过**。

NPB CG 中途段错误仍见 [测试.md](测试.md) Testcase 3
（`examples/03/npb_cg_mid_segfault/`，`--mpi=pmi2`，`--agent-match=cg.B.x`）。

---

# 04 CRLF `job.sh`

shebang 为 `#!/bin/bash\r`。必须 **exec 该脚本**（不要 `bash job.sh`，否则忽略 shebang）。

```shell
make -C /shared/agent-sidecar/examples/04
chmod +x /shared/agent-sidecar/examples/04/job.sh

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh srun -n1 \
    /shared/agent-sidecar/examples/04/job.sh
```

期望：`execution_error`；stderr 含 `bad interpreter` / `^M`。

实测：`RUN=/shared/agent-runs/20260922T030148Z-0`，exit=2，pid=0，
`execve(): bad interpreter(/bin/bash`，pack=`launch_fail`，**通过**。

---

# 05 SIGFPE（新码 `mpi_fpe`）

忙等后 `raise(SIGFPE)`，先打印 `rank N fpe`。pack 本轮为 `stub`。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/05

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_fault_fpe srun -n2 \
    /shared/agent-sidecar/examples/05/mpi_fault_fpe 10 0
```

期望：`reason_code=mpi_fpe`（即使 slurm 记 FAILED，rollup 仍用 MPI 码）。

实测：`RUN=/shared/agent-runs/20260922T030156Z-0`，exit=136，pid=2，
stderr `Floating point exception`，pack=`stub`，**通过**。

---

# 06 MPI 死锁（新码 `mpi_deadlock`）

打印 `rank N deadlock` 后 rank0 跳过 `MPI_Barrier`。**必须带 `--time`**。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/06

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_fault_deadlock srun -n2 --time=00:00:10 \
    /shared/agent-sidecar/examples/06/mpi_fault_deadlock 5 0
```

期望：stderr 有 deadlock 标记且步被 TIME LIMIT 杀时，主码仍是 **`mpi_deadlock`**
（优先于 `timeout` / `slurm_failed`）。

实测：`RUN=/shared/agent-runs/20260922T030210Z-0`，exit=143，pid=2，
stderr 有 `DUE TO TIME LIMIT`，主码 **`mpi_deadlock`**，pack=`stub`，**通过**。

---

# 06x MPI_Abort

与 [测试.md](测试.md) Testcase 1 相同，路径改为 `examples/06x/`。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/06x

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_fault_abort srun -n2 \
    /shared/agent-sidecar/examples/06x/mpi_fault_abort 10 0 1
```

实测：`RUN=/shared/agent-runs/20260922T030322Z-0`，`mpi_abort` pid=2，**通过**。

---

# 07a 应用 I/O 失败（ENOENT）

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/07a

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=io_enoent srun -n1 \
    /shared/agent-sidecar/examples/07a/io_enoent
```

期望：`execution_error`；stderr `open … No such file or directory`。若采到 PID 则
pack=`generic`；退出太快 `pid_count=0` 时 pack 会走 `launch_fail`。

实测：`RUN=/shared/agent-runs/20260922T030332Z-0`，exit=1，stderr 有 ENOENT，
pid=0 → pack=`launch_fail`。故障语义成立，采样窗口不够。

---

# 07b I/O 空转

**未实现。** 原因：`io_stall` 时段检测未做，空 Makefile。不要当必跑用例。

---

# 08 数值发散但退出 0

**未实现。** 原因：残差 NaN / 科学对错不是 sidecar 故障码，exit 0 会标 `ok`。
超出故障库契约。

---

# 09 OpenMP 超订变慢

**未实现。** 原因：`OMP_NUM_THREADS` 过大只变慢，不产生 tool anomaly。超出故障库。

---

# 10 可执行文件不存在

路径必须不存在：`examples/10/no-such-mpi`。不要创建该文件。

```shell
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh srun -n1 \
    /shared/agent-sidecar/examples/10/no-such-mpi
```

期望：`execution_error`，`pid_count=0`，pack=`launch_fail`。

实测：`RUN=/shared/agent-runs/20260922T030022Z-0`，exit=2，**通过**。

---

# 11a 非法 srun 分区

**不要**把 `-p __no_such_partition__` 写在已有 `salloc -p test` 里面：内层 `srun -p`
会被分配吃掉，hostname 会在 cn1 上成功（本次 `20260922T030338Z-0` 主码 `ok`）。

在登录节点直接：

```shell
unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
bash /shared/agent-sidecar/scripts/sidecar.sh srun -p __no_such_partition__ -n1 hostname
```

分区名可用 `AGENT_BAD_PARTITION` 覆盖。`run_fault_case.sh 11a` 已改为跳过 salloc。

期望：`execution_error`；stderr `invalid partition specified`。

实测：`RUN=/shared/agent-runs/20260922T030725Z-0`，exit=1，**通过**。

---

# 11b 合法但超订变慢

**未实现。** 原因：同 09，合法 `--ntasks` 与核数不匹配只变慢，无失败码。

---

# 12 TimeLimit

```shell
make -C /shared/agent-sidecar/examples/12
chmod +x /shared/agent-sidecar/examples/12/sleep_timeout.sh

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh srun -n1 --time=00:00:10 \
    /shared/agent-sidecar/examples/12/sleep_timeout.sh
```

期望：`reason_code=timeout`（`DUE TO TIME LIMIT` + sacct/scontrol TIMEOUT）。

实测：`RUN=/shared/agent-runs/20260922T030346Z-0`

- stderr 已有 `STEP … DUE TO TIME LIMIT`，用户 exit=143
- `events/slurm.json` 仍是 salloc 的 `JobState=RUNNING`（accounting 关闭，无 sacct 步状态）
- 主码 **`execution_error`**，不是 `timeout`

夹具能把步杀掉；本站点要把主码做成 `timeout`，需要 accounting 或让 slurm-tap
认 stderr 里的 TIME LIMIT 句（当前未做）。

---

# 13 cgroup 内存超限

复用 `examples/02/cgroup_oom`，`$(MAKE) -C examples/13`。跑法同 02。

实测：`RUN=/shared/agent-runs/20260922T030520Z-0`，主码 `ok` rss≈258MB。**同 02 站点限制**。

---

# 14 Pending 过久

**未实现。** 原因：Pending 发生在用户 `srun` 作业步之前，sidecar 不包调度等待。
超出本作业库。

---

# 15 Prolog / Epilog → `NODE_FAIL`

**未实现为 srun demo。** 原因：禁止打真实节点 / 改 Prolog。仅 unittest 注入
`NODE_FAIL` → `node_fail`（`tests/test_slurm_tap.py`）。

---

# 16 环境未 inherit

复用 01 缺库二进制 + `srun --export=NONE`（或 `examples/16/run_export_none.sh`）。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/16

salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh srun -n1 --export=NONE \
    /shared/agent-sidecar/examples/01/mpi_missing_so
```

期望：同 01，`execution_error` / `launch_fail`。

实测：`RUN=/shared/agent-runs/20260922T030558Z-0`，exit=127，**通过**。

---

# 17 节点 drain / down

**未实现。** 原因：禁止 `scontrol drain`，会破坏集群。超出契约。

---

# 18 节点宕机 / MCE

**未实现。** 原因：硬件本轮空目标；真宕机 / MCE 不可作为 demo。分类可注入 `NODE_FAIL`（同 15）。

---

# 19 网络 / IB

**未实现。** 原因：无 IB 专用码，禁止打真实网卡。硬件空目标。

---

# 20 Lustre / OST

**未实现。** 原因：禁止打真实 OST；`io_stall` 未实现。硬件空目标。

---

# 21 GPU ECC / 掉卡

**未实现。** 原因：无 GPU 采集工具。硬件空目标。

---

# 22 过热降频

**未实现。** 原因：不制造热故障，无温度码。硬件空目标。

---

# 23 电源掉电

**未实现。** 原因：同 18，整柜断电不是可复现夹具。硬件空目标。
