# 集群手工测试

登录节点 `mn`。两条入口：

- `sidecar.sh [--agent-*] srun <原生 slurm> <用户命令>`
- `sidecar-analy.sh --log DIR --code SRC`

sidecar 开关写在 `srun` **之前**。`srun` 后面就是原生 SLURM + 用户命令。
`--agent-match` 是 sidecar 选项（proc-monitor 进程名过滤），写在 `srun` 前。
省略时默认用用户二进制 basename。绝对路径不必 `--`。
`--agent-profile=tools-only` 才关闭伴随启动的模型。

脚本自己设 `PYTHONPATH`。查 run 目录用 `sidecar.sh report`，不要再
`python3 -m agent_sidecar`（除非已 export `PYTHONPATH`）。不要设
`AGENT_OPENCODE_FINAL_TIMEOUT=0`（那会跳过 wrap 期末轮模型）。

Live plot 默认 `http://127.0.0.1:8765`。笔记本：`ssh -L 8765:127.0.0.1:8765 mn`。
集群网卡直连：

```shell
unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
```

[agent live plot](http://192.168.182.136:8765/)

作业结束后：

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
echo "RUN=$RUN"
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
```

`--log` 就是这个 `$RUN`，不是 SLURM `.out`。

用例库按调研2 总表 ID 放在 `examples/<id>/`，cookbook 见
[examples/README.md](examples/README.md)。本文记录 **2026-09-23** 在 `mn` /
`cn[1-2]` 上真实跑过的命令与结论。

**结论标记**

- **通过**：夹具已实现，且 `reason_code` 与期望一致
- **实现错误**：夹具能跑，但主码/站点行为与期望不符
- **未实现**：本轮不做集群 demo（空 Makefile / 禁止打节点 / 无故障码）

---

# 集群恢复（2026-09-23）

`cn[1-2]` 曾 `idle*` / `Not responding`。恢复：

```shell
scontrol update NodeName=cn[1-2] State=RESUME Reason=operator-resume
sinfo -N -l
timeout 20 salloc -N1 -n1 -w cn1 -p test --time=00:00:15 hostname
```

实跑前 `sinfo`：`cn1`/`cn2` `idle`，partition `test`。跑完后两节点仍 `idle`。

代码已 rsync 到 `mn:/shared/agent-sidecar`。编译：

```shell
CC=mpicc make -C /shared/agent-sidecar/examples
```

分类验收为缩短墙钟，本批设了 `AGENT_OPENCODE_FINAL_TIMEOUT=0`（跳过 wrap 期末
OpenCode）。要看中文 `assist/job.json` LLM 总结时请 `unset`。
`health` 的 IO 秒用了 `8`（cookbook 默认 `15`）。

```shell
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
```

---

# 2026-09-23 实跑总表

| ID | 期望 `reason_code` | 实测 | 结论 | RUN |
| --- | --- | --- | --- | --- |
| health | ok | ok，pid=2，exit=0 | **通过** | `20260923T074018Z-0` |
| 01 | execution_error | execution_error，pid=0，exit=127 | **通过** | `20260923T074037Z-0` |
| 02 | slurm_oom | **ok**，pid=1，rss≈258MB，exit=0 | **实现错误** | `20260923T074043Z-0` |
| 2a | node_local | 未 srun | **未实现**（禁止打节点；仅 unittest 注入） | — |
| 03 | mpi_segfault | mpi_segfault，pid=2，exit=139 | **通过** | `20260923T074124Z-0` |
| 03 NPB | mpi_segfault | 本次未重跑（无 `examples/03/cg.B.x`） | **未在本次实跑** | — |
| 04 | execution_error | execution_error，pid=0，exit=2 | **通过** | `20260923T074141Z-0` |
| 05 | mpi_fpe | mpi_fpe，pid=2，exit=136 | **通过** | `20260923T074146Z-0` |
| 06 | mpi_deadlock | mpi_deadlock，pid=2，exit=143 | **通过** | `20260923T074204Z-0` |
| 06x | mpi_abort | mpi_abort，pid=2，exit=1 | **通过** | `20260923T074329Z-0` |
| 07a | execution_error | execution_error，pid=0，exit=1 | **通过**（过快未采到 pid，像 launch_fail） | `20260923T074347Z-0` |
| 07b | io_stall | 未 srun | **未实现**（`io_stall` 时段检测未做） | — |
| 08 | （exit 0） | 未 srun | **未实现**（数值发散不是 sidecar 故障码） | — |
| 09 | — | 未 srun | **未实现**（OpenMP 超订只变慢） | — |
| 10 | execution_error | execution_error，pid=0，exit=2 | **通过** | `20260923T074352Z-0` |
| 11a | execution_error | execution_error，pid=0，exit=1 | **通过** | `20260923T074357Z-0` |
| 11b | — | 未 srun | **未实现**（合法超订只变慢） | — |
| 12 | timeout | **execution_error**，pid=0，exit=143 | **实现错误** | `20260923T074402Z-0` |
| 13 | slurm_oom | **ok**，pid=1，rss≈258MB，exit=0 | **实现错误** | `20260923T074527Z-0` |
| 14 | — | 未 srun | **未实现**（Pending 在 sidecar 包装之前） | — |
| 15 | node_fail | 未 srun | **未实现**（禁止 Prolog 打节点；仅 unittest） | — |
| 16 | execution_error | execution_error，pid=0，exit=127 | **通过** | `20260923T074605Z-0` |
| 17 | — | 未 srun | **未实现**（禁止 `scontrol drain`） | — |
| 18–23 | — | 未 srun | **未实现**（硬件空目标） | — |

实现错误原因（站点，不是夹具没编译）：

- **02 / 13**：`sacct` 关闭、`--mem=16M` 不进 cgroup 杀进程，malloc 256MiB 成功退出。
- **12**：stderr 仍可能有 `DUE TO TIME LIMIT`，但 `events/slurm.json` 停在 salloc 的 `RUNNING`，主码落成 `execution_error`，且 `pid_count=0`。

---



# 正常用例（health）— 通过

`--agent-match=mpi_io_load`。本次实跑 IO 秒=`8`。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_io_load \
    --agent-output-dir /shared/agent-runs \
    srun -n2 \
    /shared/agent-sidecar/examples/mpi_io_load 8 /shared/mpi-io 0
```

cookbook 默认 `15` 秒、且 `unset AGENT_OPENCODE_FINAL_TIMEOUT`：

```shell
unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_io_load srun -n2 \
    /shared/agent-sidecar/examples/mpi_io_load 15 /shared/mpi-io 0
```

看 live plot 可用更长 IO（60s）：

```shell
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_io_load srun -n2 \
    /shared/agent-sidecar/examples/mpi_io_load 60 /shared/mpi-io
```

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
cat "$RUN/assist/job.json"
```

期望：`reason_code=ok`，`pid_count>0`。
结论：**通过**（`20260923T074018Z-0`，pid=2，exit=0）。

---



# 01 缺动态库 — 通过

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/01
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_missing_so \
    --agent-output-dir /shared/agent-runs \
    srun -n1 \
    /shared/agent-sidecar/examples/01/mpi_missing_so
```

期望：`execution_error`，`pid_count=0`，pack=`launch_fail`；stderr 含
`cannot open shared object file`。
结论：**通过**（`20260923T074037Z-0`，exit=127）。

---



# 02 / 2b cgroup malloc — 实现错误

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/02
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N1 -n1 -w cn1 -p test --mem=16M \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=cgroup_oom \
    --agent-output-dir /shared/agent-runs \
    srun -n1 --mem=16M \
    /shared/agent-sidecar/examples/02/cgroup_oom
```

期望（站点强制 cgroup 时）：`slurm_oom`。
结论：**实现错误**（`20260923T074043Z-0`，实测 `ok`，rss≈257.9MB，exit=0；
本集群 `--mem` 不杀进程，不要为了出码打满节点）。

---



# 2a 内核 OOM Killer — 未实现

**不要 srun。** 会打节点。仅 unittest 注入 `Killed process` → `node_local`。
结论：**未实现**（禁止集群实跑）。

---



# 03 段错误 — 通过

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/03
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_fault_segfault \
    --agent-output-dir /shared/agent-runs \
    srun -n2 \
    /shared/agent-sidecar/examples/03/mpi_fault_segfault 10 0
```

作业结束后授权源码：

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/agent-sidecar/examples
```

期望：`mpi_segfault`，`pid_count>0`。
结论：**通过**（`20260923T074124Z-0`，pid=2，exit=139）。

## 03 NPB CG 中途段错误 — 未在本次实跑

```shell
export NPB_MPI_ROOT=/shared/NPB3.4.3/NPB3.4-MPI
export AGENT_SHARED=/shared
bash /shared/agent-sidecar/examples/03/npb_cg_mid_segfault/build.sh

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh \
    --agent-verbose \
    --agent-skills=proc-monitor,slurm-tap,mpi-scan,node-diag,eth-monitor \
    --agent-match=cg.B.x \
    srun --mpi=pmi2 -n2 \
    /shared/agent-sidecar/examples/03/cg.B.x
```

`--code` 指向打过补丁的树：

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/npb-build/cg-mid-segfault
```

期望：`mpi_segfault`；stderr `rank 0 segfault (null deref)`。
结论：**未在本次实跑**（`examples/03/cg.B.x` 不存在，未跑 `build.sh`）。

---



# 04 CRLF `job.sh` — 通过

必须 **exec** 该脚本（不要 `bash job.sh`）。

```shell
make -C /shared/agent-sidecar/examples/04
chmod +x /shared/agent-sidecar/examples/04/job.sh
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose \
    --agent-output-dir /shared/agent-runs \
    srun -n1 \
    /shared/agent-sidecar/examples/04/job.sh
```

期望：`execution_error`；stderr `bad interpreter`。
结论：**通过**（`20260923T074141Z-0`，exit=2）。

---



# 05 SIGFPE — 通过

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/05
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_fault_fpe \
    --agent-output-dir /shared/agent-runs \
    srun -n2 \
    /shared/agent-sidecar/examples/05/mpi_fault_fpe 10 0
```

期望：`mpi_fpe`（rollup 优先于 `slurm_failed`）。
结论：**通过**（`20260923T074146Z-0`，pid=2，exit=136）。

---



# 06 MPI 死锁 — 通过

**必须带 `--time`。**

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/06
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_fault_deadlock \
    --agent-output-dir /shared/agent-runs \
    srun -n2 --time=00:00:10 \
    /shared/agent-sidecar/examples/06/mpi_fault_deadlock 5 0
```

期望：主码 `mpi_deadlock`。
结论：**通过**（`20260923T074204Z-0`，pid=2，exit=143）。

---



# 06x MPI_Abort（原 Testcase 1）— 通过

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/06x
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_fault_abort \
    --agent-output-dir /shared/agent-runs \
    srun -n2 \
    /shared/agent-sidecar/examples/06x/mpi_fault_abort 10 0 1
```

```shell
RUN=$(ls -1dt /shared/agent-runs/20* | head -1)
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/agent-sidecar/examples
```

期望：`mpi_abort`，`pid_count>0`。
结论：**通过**（`20260923T074329Z-0`，pid=2，exit=1）。

---



# 07a 应用 I/O ENOENT — 通过

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/07a
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=io_enoent \
    --agent-output-dir /shared/agent-runs \
    srun -n1 \
    /shared/agent-sidecar/examples/07a/io_enoent
```

期望：`execution_error`；stderr `No such file or directory`。退出太快时
`pid_count=0`。
结论：**通过**（`20260923T074347Z-0`，pid=0，exit=1；与 launch_fail 难以区分）。

---



# 07b I/O 空转 — 未实现

`io_stall` 时段检测未做，Makefile `all` 为空。结论：**未实现**。

---



# 08 数值发散但退出 0 — 未实现

残差 NaN 不是 sidecar 故障码。结论：**未实现**。

---



# 09 OpenMP 超订变慢 — 未实现

无 tool anomaly。结论：**未实现**。

---



# 10 可执行文件不存在 — 通过

不要创建 `examples/10/no-such-mpi`。

```shell
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose \
    --agent-output-dir /shared/agent-runs \
    srun -n1 \
    /shared/agent-sidecar/examples/10/no-such-mpi
```

期望：`execution_error`，`pid_count=0`，pack=`launch_fail`。
结论：**通过**（`20260923T074352Z-0`，exit=2）。

---



# 11a 非法 srun 分区 — 通过

**不要**把 `-p __no_such_partition__` 写进已有 `salloc -p test`。登录节点直接：

```shell
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose \
  --agent-output-dir /shared/agent-runs \
  srun -p __no_such_partition__ -n1 hostname
```

期望：`execution_error`；stderr `invalid partition specified`。
结论：**通过**（`20260923T074357Z-0`，exit=1）。

---



# 11b 合法但超订变慢 — 未实现

结论：**未实现**。

---



# 12 TimeLimit — 实现错误

```shell
make -C /shared/agent-sidecar/examples/12
chmod +x /shared/agent-sidecar/examples/12/sleep_timeout.sh
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose \
    --agent-output-dir /shared/agent-runs \
    srun -n1 --time=00:00:10 \
    /shared/agent-sidecar/examples/12/sleep_timeout.sh
```

期望：`timeout`。
结论：**实现错误**（`20260923T074402Z-0`，实测 `execution_error`，pid=0，exit=143；
accounting 关闭时 slurm-tap 看不到 TIMEOUT）。

---



# 13 cgroup 内存超限 — 实现错误

复用 `02/cgroup_oom`。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/13
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N1 -n1 -w cn1 -p test --mem=16M \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=cgroup_oom \
    --agent-output-dir /shared/agent-runs \
    srun -n1 --mem=16M \
    /shared/agent-sidecar/examples/02/cgroup_oom
```

期望：`slurm_oom`。
结论：**实现错误**（`20260923T074527Z-0`，实测 `ok`，rss≈257.9MB；同 02）。

---



# 14 Pending 过久 — 未实现

Pending 在用户 `srun` 作业步之前。结论：**未实现**。

---



# 15 Prolog / Epilog → NODE_FAIL — 未实现

**不要 srun。** 仅 unittest 注入。结论：**未实现**。

---



# 16 环境未 inherit — 通过

```shell
CC=mpicc make -C /shared/agent-sidecar/examples/16
export AGENT_OPENCODE_FINAL_TIMEOUT=0
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose \
    --agent-output-dir /shared/agent-runs \
    srun -n1 --export=NONE \
    /shared/agent-sidecar/examples/01/mpi_missing_so
```

期望：同 01，`execution_error` / `launch_fail`。
结论：**通过**（`20260923T074605Z-0`，exit=127）。

---



# 17 节点 drain / down — 未实现

禁止 `scontrol drain`。结论：**未实现**。

---



# 18–23 硬件 — 未实现

空 Makefile 目标；真宕机 / MCE / 网卡坏不可作为 demo。结论：**未实现**。
