# 集群手工测试

登录节点 `mn`。两条入口：

- `sidecar.sh [--agent-*] srun <原生 slurm> <用户命令>`
- `sidecar-analy.sh --log DIR --code SRC`

sidecar 开关写在 `srun` **之前**。`srun` 后面就是原生 SLURM + 用户命令。
`--agent-match` 是 sidecar 选项（proc-monitor 进程名过滤），写在 `srun` 前。
省略时默认用用户二进制 basename。绝对路径不必 `--`。
`--agent-profile=tools-only` 才关闭伴随启动的模型。

脚本自己设 `PYTHONPATH`。查 run 目录用 `sidecar.sh report`，不要再
`python3 -m agent_sidecar`（除非已 export `PYTHONPATH`）。不要设
`AGENT_OPENCODE_FINAL_TIMEOUT=0`（那会跳过 wrap 期末轮模型）。

Live plot 默认 `http://127.0.0.1:8765`。笔记本：`ssh -L 8765:127.0.0.1:8765 mn`。
集群网卡直连：

```shell
unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
```

[agent live plot](http://192.168.182.136:8765/)

作业结束后：

```shell
RUN=$(ls -1dt /shared/agent-runs/* | head -1)
echo "RUN=$RUN"
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
```

`--log` 就是这个 `$RUN`，不是 SLURM `.out`。

---



# 正常用例

伴随 `srun` 启动 sidecar（CPU/RSS/IO、OOM、SLURM/MPI）。健康作业没有 live
异常触发，作业结束后应由期末 OpenCode 读取 telemetry 并写出 job-assist 中文总结
（`assist/job.json`），不是预制 resource hints。
`--agent-match=mpi_io_load` 过滤采集进程名。

```shell
unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_io_load srun -n2 \
    /shared/agent-sidecar/examples/mpi_io_load 15 /shared/mpi-io 0
```

看 live plot 可用更长 IO（60s）。sidecar 开关都写在 `srun` 前：

```shell
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-verbose --agent-match=mpi_io_load srun -n2 \
    /shared/agent-sidecar/examples/mpi_io_load 60 /shared/mpi-io
```

```shell
RUN=$(ls -1dt /shared/agent-runs/* | head -1)
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
cat "$RUN/assist/job.json"
```

期望：终端出现期末 OpenCode 启动/心跳与耗时信息；`reason_code=ok`，
`pid_count>0`，`series/` 有 JSONL；OpenCode prompt 使用 telemetry 的
`summary`、`anomalies`、`reason_code`、`evidence_paths`（不嵌入 JSONL，也不
附加 PNG）；report 中有「1. 结论：…」分条，且 LLM 写出的
`assist/job.json` 存在。正常作业不需要 `--code`。

---



# Testcase 1 异常状态（MPI Abort）

`10 0 1` = 工作 10 秒后 rank 0 `MPI_Abort(..., 1)`。
`--agent-match=mpi_fault_abort` 过滤 proc-monitor 进程名。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples mpi_fault_abort

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_fault_abort srun -n2 \
    /shared/agent-sidecar/examples/mpi_fault_abort 10 0 1
```

需要启动痕迹：`sidecar.sh --agent-verbose --agent-match=mpi_fault_abort srun -n2 …`。

作业结束后授权源码（默认 LLM）：

```shell
RUN=$(ls -1dt /shared/agent-runs/* | head -1)
echo "RUN=$RUN"
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/agent-sidecar/examples
cat "$RUN/assist/analysis.json"
cat "$RUN/assist/job.json"
```

只要 pack、不调模型时加 `--no-llm`。

期望：wrap 后已有中文总结与 `analy: sidecar-analy.sh --log …` 提示；
`--code` 后 `reason_code=mpi_abort`，`pid_count>0`，`needs_source=false`，
可引用 `examples` 树内命中。

---



# Testcase 2 段错误

`10 0` = 工作 10 秒后 rank 0 段错误。
`--agent-match=mpi_fault_segfault` 过滤 proc-monitor 进程名。

```shell
CC=mpicc make -C /shared/agent-sidecar/examples mpi_fault_segfault

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh --agent-match=mpi_fault_segfault srun -n2 \
    /shared/agent-sidecar/examples/mpi_fault_segfault 10 0
```

作业结束后：

```shell
RUN=$(ls -1dt /shared/agent-runs/* | head -1)
echo "RUN=$RUN"
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/agent-sidecar/examples
cat "$RUN/assist/analysis.json"
cat "$RUN/assist/job.json"
```

期望：wrap 后已有中文总结；`--code` 后 `reason_code=mpi_segfault`，
`pid_count>0`，可引用 `examples` 树内命中；不要编造未授权 `file:line`。

---



# Testcase 3 NPB 异常退出（CG 中途段错误）

NAS NPB 3.4-MPI **CG Class B**，两 rank。补丁在 `timer_start(1)` 后记录
`crash_t0 = mpi_wtime()`，每次 `conj_grad` 后若墙钟 ≥ 30s：rank 0 先向
stderr 打印 `rank 0 segfault (null deref)` 再空指针解引用；其余 rank
`sleep(3)` 后 `exit(0)`，避免 SLURM 挂死。不要用 `timer_read(1)`（NPB 3.4
在 `timer_stop` 前一直为 0）。原树 `/shared/NPB3.4.3` 不改；编译拷贝在
`/shared/npb-build/cg-mid-segfault`。二进制 comm 超过 15 字节，用
`--agent-match=cg.B.x`。通信负载加 `eth-monitor`。
`srun` 必须加 `--mpi=pmi2`（或 pmix），否则两个任务各自 `nprocs=1`。

不要在 WSL 上当集群跑。需要 `gfortran`/`mpif90`（mn 用 `gfortran-15` 包装）。

```shell
export NPB_MPI_ROOT=/shared/NPB3.4.3/NPB3.4-MPI
export AGENT_SHARED=/shared
bash /shared/agent-sidecar/examples/npb_cg_mid_segfault/build.sh
# 产出 /shared/agent-sidecar/examples/cg.B.x

unset AGENT_OPENCODE_FINAL_TIMEOUT
export AGENT_LIVE_PLOT_HOST=0.0.0.0
export MPLBACKEND=Agg
salloc -N2 -n2 -w cn[1-2] -p test \
  bash /shared/agent-sidecar/scripts/sidecar.sh \
    --agent-verbose \
    --agent-skills=proc-monitor,slurm-tap,mpi-scan,node-diag,eth-monitor \
    --agent-match=cg.B.x \
    srun --mpi=pmi2 -n2 \
    /shared/agent-sidecar/examples/cg.B.x
```

墙钟大约 30s 出故障（不是跑完整个 Class B）。作业结束后 `--code` 必须指向
**打过补丁的** Fortran 树，不要指干净的 `/shared/NPB3.4.3`：

```shell
RUN=$(ls -1dt /shared/agent-runs/* | head -1)
echo "RUN=$RUN"
bash /shared/agent-sidecar/scripts/sidecar.sh report --run-dir "$RUN"
bash /shared/agent-sidecar/scripts/sidecar-analy.sh \
  --log "$RUN" --code /shared/npb-build/cg-mid-segfault
cat "$RUN/events/stderr.tail"
cat "$RUN/assist/analysis.json"
cat "$RUN/assist/job.json"
```

期望：`events/stderr.tail` 含 `rank 0 segfault (null deref)`；
`reason_code=mpi_segfault`，`pid_count>0`；`--code` 后可引用
`CG/cg.f90` 中 `c_null_ptr` / 空指针那一行；不要编造未授权 `file:line`。
原版未打补丁的 `cg.B.x` 在 `/shared/NPB3.4.3/NPB3.4-MPI/bin/`，本用例不要用它。